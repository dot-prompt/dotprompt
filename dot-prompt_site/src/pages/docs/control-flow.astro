---
import DocsLayout from '../../layouts/DocsLayout.astro';
import CodeBlock from '../../components/CodeBlock.astro';

const ifElseCode = `if @is_premium is true do
This is premium content.
You get extra details.
else
This is free content.
Upgrade to premium for more!
end @is_premium`;

const chainedCode = `if @age below 18 do
You must be 18+ to use this.
elif @age below 21 do
Some features restricted.
else
Full access granted.
end @age`;

const caseCode = `case @tier do
free: Basic support only.
pro: Priority support during business hours.
enterprise: 24/7 dedicated support.
end @tier`;

const caseIntCode = `case @step do
1: Start with the basics.
2: Build on step 1.
3: Add more complexity.
4: Refine your approach.
5: Complete the process.
end @step`;

const varyCode = `vary @variation do
version_a: Try this approach first.
version_b: Try this alternative.
version_c: Here is another way.
end @variation`;

const compileCode = `# Input: tier=pro, is_premium=true
case @tier do
free: Basic support.
pro: Priority support during business hours.
enterprise: 24/7 dedicated support.
end @tier`;

const compiledOutput = `Priority support during business hours.`;
---

<DocsLayout 
  title="Control Flow" 
  activeSlug="/docs/control-flow"
  description="if/elif/else, case, and vary blocks in .prompt"
>
  <h1>Control Flow</h1>
  <p>
    .prompt supports three control flow constructs: <code>if</code>, <code>case</code>, and <code>vary</code>. All resolve at compile time.
  </p>

  <h2>If / Elif / Else</h2>
  <p>
    Use <code>if</code> for conditional content:
  </p>

  <CodeBlock code={ifElseCode} language="dotprompt" />

  <h3>Operators</h3>
  <table>
    <thead>
      <tr>
        <th>Operator</th>
        <th>Meaning</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>is</code></td>
        <td>Equality</td>
      </tr>
      <tr>
        <td><code>not</code></td>
        <td>Inequality</td>
      </tr>
      <tr>
        <td><code>above</code></td>
        <td>Greater than</td>
      </tr>
      <tr>
        <td><code>below</code></td>
        <td>Less than</td>
      </tr>
      <tr>
        <td><code>min</code></td>
        <td>Greater than or equal</td>
      </tr>
      <tr>
        <td><code>max</code></td>
        <td>Less than or equal</td>
      </tr>
      <tr>
        <td><code>between</code></td>
        <td>Inclusive range</td>
      </tr>
    </tbody>
  </table>

  <h3>Chained Conditions</h3>
  <CodeBlock code={chainedCode} language="dotprompt" />

  <h2>Case</h2>
  <p>
    Use <code>case</code> for deterministic branch selection based on enum values:
  </p>

  <CodeBlock code={caseCode} language="dotprompt" />

  <h3>With Bounded Integers</h3>
  <CodeBlock code={caseIntCode} language="dotprompt" />

  <h2>Vary</h2>
  <p>
    Use <code>vary</code> for random or seeded variation. Perfect for A/B testing or adding variety:
  </p>

  <CodeBlock code={varyCode} language="dotprompt" />

  <p>
    Pass a <code>seed</code> parameter to get deterministic results. Without seed, each call selects randomly.
  </p>

  <h2>Compile-Time Resolution</h2>
  <p>
    All control flow resolves at compile time. The LLM receives only the selected branch:
  </p>

  <CodeBlock code={compileCode} language="dotprompt" />

  <p>
    Compiles to:
  </p>

  <CodeBlock code={compiledOutput} language="dotprompt" />

  <h2>Next Steps</h2>
  <ul>
    <li><a href="/docs/fragments">Fragments</a> — Compose from reusable pieces</li>
    <li><a href="/docs/versioning">Versioning</a> — Manage changes over time</li>
  </ul>
</DocsLayout>
