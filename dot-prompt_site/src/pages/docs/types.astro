---
import DocsLayout from '../../layouts/DocsLayout.astro';
import CodeBlock from '../../components/CodeBlock.astro';

const stringParamCode = `params:
  @user_name: str`;

const integerParamCode = `params:
  @age: int`;

const boundedIntCode = `params:
  @step: int[1..5]`;

const boolParamCode = `params:
  @is_premium: bool = false`;

const enumParamCode = `params:
  @tier: enum[free, pro, enterprise] = free`;

const listParamCode = `params:
  @skills: list[communication, analysis, creativity]`;

const defaultParamCode = `params:
  @depth: enum[shallow, medium, deep] = medium`;

const docParamCode = `params:
  @name: str -> The user's display name
  @age: int[18..120] -> User's age (must be 18+)`;
---

<DocsLayout 
  title="Types" 
  activeSlug="/docs/types"
  description="Type system documentation for .prompt"
>
  <h1>Types</h1>
  <p>
    The .prompt language has a type system that validates parameters and drives compile-time branching.
  </p>

  <h2>Type Categories</h2>
  <table>
    <thead>
      <tr>
        <th>Type</th>
        <th>Lifecycle</th>
        <th>Notes</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>str</code></td>
        <td>Runtime</td>
        <td>Cannot drive branching</td>
      </tr>
      <tr>
        <td><code>int</code></td>
        <td>Runtime</td>
        <td>Cannot drive branching</td>
      </tr>
      <tr>
        <td><code>int[a..b]</code></td>
        <td>Compile-time</td>
        <td>Bounded integer</td>
      </tr>
      <tr>
        <td><code>bool</code></td>
        <td>Compile-time</td>
        <td>Boolean</td>
      </tr>
      <tr>
        <td><code>enum[a, b, c]</code></td>
        <td>Compile-time</td>
        <td>Single value from list</td>
      </tr>
      <tr>
        <td><code>list[a, b, c]</code></td>
        <td>Compile-time</td>
        <td>Multiple values</td>
      </tr>
    </tbody>
  </table>

  <h2>Compile-Time vs Runtime</h2>
  <p>
    Types marked as <strong>compile-time</strong> can be used in <code>if</code>, <code>case</code>, and <code>vary</code> blocks. The compiler evaluates these at compile time and discards untaken branches.
  </p>
  <p>
    Types marked as <strong>runtime</strong> cannot drive branching. They are injected into the compiled prompt but don't affect which branches are included.
  </p>

  <h2>Examples</h2>

  <h3>String</h3>
  <CodeBlock code={stringParamCode} language="dotprompt" />

  <h3>Integer</h3>
  <CodeBlock code={integerParamCode} language="dotprompt" />

  <h3>Bounded Integer (for loop control)</h3>
  <CodeBlock code={boundedIntCode} language="dotprompt" />

  <h3>Boolean</h3>
  <CodeBlock code={boolParamCode} language="dotprompt" />

  <h3>Enum</h3>
  <CodeBlock code={enumParamCode} language="dotprompt" />

  <h3>List</h3>
  <CodeBlock code={listParamCode} language="dotprompt" />

  <h2>Defaults</h2>
  <p>
    Parameters can have default values:
  </p>
  <CodeBlock code={defaultParamCode} language="dotprompt" />

  <h2>Documentation</h2>
  <p>
    Add documentation with <code>-&gt;</code>:
  </p>
  <CodeBlock code={docParamCode} language="dotprompt" />

  <h2>Next Steps</h2>
  <ul>
    <li><a href="/docs/control-flow">Control Flow</a> — Use types in branching logic</li>
    <li><a href="/docs/language-reference">Language Reference</a> — Full syntax</li>
  </ul>
</DocsLayout>
